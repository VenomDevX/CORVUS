{
  "name": "corvus",
  "productName": "Corvus",
  "version": "0.1.0",
  "private": true,
  "description": "Corvus — Windows desktop AI assistant",
  "author": "Corvus",
  "type": "module",
  "scripts": {
    "guard": "node scripts/naming-guard.mjs",
    "audit": "npm audit --audit-level=high && npm audit --prefix frontend --audit-level=high",
    "assets": "node scripts/export-assets.mjs",
    "dev": "npm run dev --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "test": "npm run guard && npm run test --prefix frontend && npm run test:backend",
    "test:backend": "cd backend && .venv\\Scripts\\python.exe -m pytest -q",
    "dist:backend": "cd backend && set OPENBLAS_NUM_THREADS=1&& set OMP_NUM_THREADS=1&& set MKL_NUM_THREADS=1&& .venv\\Scripts\\python.exe -m PyInstaller ../installer/corvus-backend.spec --distpath ../dist-backend --workpath ../build/pyinstaller --noconfirm",
    "dist": "npm run dist:backend && npm run dist --prefix frontend",
    "precommit": "npm run guard",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "png-to-ico": "^2.1.8",
    "sharp": "^0.35.3"
  }
}
